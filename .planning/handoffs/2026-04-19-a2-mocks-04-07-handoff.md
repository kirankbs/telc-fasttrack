# Handoff: A2 Mocks 04-07 (Essen, Termine, Arbeit, Freizeit)

**Issue:** #25
**PR:** #32 — https://github.com/kirankbs/telc-fasttrack/pull/32
**Branch:** `a2-mocks-04-07`
**CI:** all green (Typecheck+Tests, E2E, Vercel Preview)

## What was built

4 complete A2 mock exams replacing stubs:

| File | Theme | Questions |
|------|-------|-----------|
| `mock_04.json` | Essen & Trinken | 30 |
| `mock_05.json` | Termine | 30 |
| `mock_06.json` | Arbeit & Beruf | 30 |
| `mock_07.json` | Freizeit | 30 |

Total: 120 unique question IDs. Zero duplicates within or across mocks 01–07.

Each mock: 3×5 Listening MCQ (parts 1-3) + 5 matching + 5 true/false + 5 MCQ (Reading parts 1-3) + form_fill + short_message (Writing) + introduce + conversation + planning (Speaking).

## Side fix included

Added `[]` empty-array stubs for:
- `apps/mobile/src/data/grammar/A2_grammar.json` (overwrite when #28 grammar PR merges — stub is harmless)
- `apps/mobile/src/data/grammar/B1_grammar.json`
- `apps/mobile/src/data/grammar/B2_grammar.json`
- `apps/mobile/src/data/grammar/C1_grammar.json`
- `apps/mobile/src/data/vocabulary/A2_vocabulary.json` (overwrite when #27 vocab PR merges)
- `apps/mobile/src/data/vocabulary/B1_vocabulary.json`
- `apps/mobile/src/data/vocabulary/B2_vocabulary.json`
- `apps/mobile/src/data/vocabulary/C1_vocabulary.json`

Root cause: grammar hub (PR #23) and vocab page both iterate all 5 levels at build time. Next.js's file tracer calls `lstat` on all resolved paths during `next build`. Missing files caused the Vercel deployment to error. This was not a problem before PR #23 shipped.

When the real content PRs (#27 vocabulary, #28 grammar, and future B1-C1 content) merge, they will overwrite these stubs with real content.

## Validation

- Python validation script: 10/10 structural checks per mock (IDs unique, version=1, _stub removed, counts correct, correctAnswer in options, explanations non-empty, audioTimestamp present, writing/speaking counts)
- `pnpm typecheck`: 2/2 packages clean
- CI: Typecheck+Tests pass 35s, E2E pass 42s, Vercel pass

## Notes for orchestrator

- Mocks 01-07 now complete. Next: mocks 08-10 (#26) to finish the A2 set.
- The stub files added here will be naturally overwritten when the a2-vocabulary and a2-grammar branches merge — no cleanup needed.
- Other content branches (a2-mocks-08-10, etc.) will hit the same Vercel issue if they branch from main before the stub fix merges. Recommend merging this PR first.
