// Re-export all design tokens from the shared @fastrack/config package.
export {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  MIN_TOUCH_TARGET,
  LEVEL_CONFIG,
} from '@fastrack/config';
